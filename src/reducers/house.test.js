import { loading, houses, error } from './house'
import { isLoading, hasErrored, storeHouse } from '../actions'

describe('house', () => {
  describe('loading', () => {
    it('should update loading if the action type is IS_LOADING', () => {
      
      
    }
  })
})